-- script that creates a stored procedure AddBonus 
-- that adds a new correction for a student.
DELIMITER $$;
CREATE PROCEDURE AddBonus(IN user_id, project_name, score)
BEGIN
IF NOT project_name
project_name = projects.name
END IF
END;

